<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.example.auto_warehouse.mapper.IncomeMapper">

    <update id="updateIncome">
        update auto_warehouseDB.income set income = income+#{money}
        where round(yearMonth/100, 0)=#{year} and yearMonth-round(yearMonth/100, 0)*100=#{month};
    </update>
</mapper>